source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins

# Ruby 3.4+ dropped these from default gems; the github-pages gem's
# pinned (older) Jekyll/dependency versions still expect them present.
gem "csv"
gem "base64"
gem "bigdecimal"
gem "logger"
gem "webrick"
